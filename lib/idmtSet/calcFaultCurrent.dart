import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class FaultCurrentScreen extends StatefulWidget {
  _FaultCurrentState createState() => _FaultCurrentState();
}

class _FaultCurrentState extends State<FaultCurrentScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: new Text("Calculate TMS"),
      ),
      body: new Container(),
    );
  }
}